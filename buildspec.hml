version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do (pre_build)
  build:
    commands:
      - echo Nothing to do (build)
  post_build:
    commands:
      - echo Nothing to do (post)
